# The GitHub workflow replaces this marker with linux_sunxi64_defconfig plus
# the board's required MFD_AC200 setting before BitBake parses the layer.
# linux-orangepi fails configuration if this marker was not replaced.
